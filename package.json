{
  "name": "gov-rss",
  "version": "1.0.0",
  "scripts": {
    "dev": "next",
    "build": "next build",
    "start": "next start",
    "export": "next export",
    "deploy": "rm -rf node_modules/.cache && next build && next export && touch out/.nojekyll && git add out/ && git commit -m \"Deploy gov-rss to gh-pages\" && git subtree push --prefix out origin gh-pages"
  },
  "dependencies": {
    "next": "^10.1",
    "react": "^17.0.1",
    "react-dom": "^17.0.1"
  },
  "license": "MIT",
  "devDependencies": {
    "autoprefixer": "^10.2.5",
    "babel-plugin-transform-define": "^1.3.0",
    "jest": "^26.6.3",
    "postcss": "^8.2.9",
    "prettier": "2.2.1",
    "tailwindcss": "^2.1.1"
  }
}
